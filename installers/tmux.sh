# TPM is deprecated
# tpack init is an alternative but did not show a UI when I tested on OSX
mkdir -p ~/.config/tmux/plugins
git clone https://github.com/niksingh710/minimal-tmux-status ~/.config/tmux/plugins/minimal-tmux-status
