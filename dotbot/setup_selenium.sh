# to install selenium that runs from a CLI

# npm install -g selenium-standalone
# sudo pacman -S jdk-openjdk
# sudo selenium-standalone install


# to start selenium:
#selenium-standalone start --debug-port=5858
