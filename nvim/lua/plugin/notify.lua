vim.notify = require("notify")
-- require("notify").setup({
-- 	background_colour = "#000000", -- as base16 colorscheme does not define Normal? to avoid warning notification at nvim startup
-- 	max_width = vim.o.columns / 2,
-- 	max_height = vim.o.lines / 2,
-- 	stages = "static",
-- })
